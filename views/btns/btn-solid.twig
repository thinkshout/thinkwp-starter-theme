{# Solid button
 * A color pill button.
 * Variations styled below.
  DEFAULT STYLES SET IN: tailwind-btns.json.

 * COLOR OPTIONS:
 *    - yellow-500 (default)
 *    - black
 *    - gray-200
 *
 * SIZE OPTIONS:
 *    - lg (default)
 *    - md
 *    - sm
 *    - xs

  HOW TO USE:
  Gray btn-solid md:
  {% include 'btns/btn-solid.twig' with {
      link: {title: 'learn more', url: '/'}
      size: 'md',
      color: 'grey-200'
    }
  %}
#}

{# Primary values #}
{% set link = link %}
{% set size = size ?: 'md' %}
{% set color = color ?: 'btn-primary' %}
{% set add_classes = add_classes %}
{% set link_svg_name = link_svg_name %}

{# btn classes, colorway, svg name #}
{% set btn_class = "btn-solid btn--" ~ size ~ " " ~ color %}
{# Final set of classes #}
{% set link_classes = add_classes ? btn_class ~ ' ' ~ add_classes : btn_class %}

{% extends 'btns/btn-base.twig' %}
