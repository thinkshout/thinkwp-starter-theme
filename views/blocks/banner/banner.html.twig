{# Banner -- left aligned
 *
 * Configuration options:
 *  image align:
 *   - Left | left
 *   - Right | right
 *   Inset or full width
 *   align_img (left, right (default)
 *
 * Fields:
 * - eyebrow
 * - title
 * - text
 * - primary_btn
 * - secondary_btn
 * - presentation
 * - image
 * - is_hero
#}

{% if "left" in presentation %}
  {% include "blocks/banner/banner--side-text.twig" with {
    image_align: "right"
  } %}
{% elseif "right" in presentation %}
  {% include "blocks/banner/banner--side-text.twig" with {
    image_align: "left"
  } %}
{% elseif "floating" in presentation %}
  {% include "blocks/banner/banner--overlay.twig" %}
{% else %}
  {% include "blocks/banner/banner--center.twig" %}
{% endif %}